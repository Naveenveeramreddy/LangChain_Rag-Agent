# LangChain_Rag-Agent
A lightweight RAG and semantic search system using LangChain. It loads PDFs, creates vector embeddings, stores them in an in-memory vector DB, and retrieves relevant chunks for queries. Integrated with a local Ollama LLM for context-aware question answering.
